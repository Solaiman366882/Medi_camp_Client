import { Helmet } from "react-helmet";

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Atom | Dashboard</title>
            </Helmet>
            this is dashboard
        </div>
    );
};

export default Dashboard;